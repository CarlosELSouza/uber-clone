import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Profile = () => {
  return (
    <SafeAreaView>
      <View>
        <Text className="text-zinc-500">Home</Text>
      </View>
    </SafeAreaView>
  );
};

export default Profile;
